/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}, // Include autoprefixer for cross-browser compatibility
  },
};

export default config;
